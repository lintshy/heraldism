# Spring Boot Application Properties
# This file is kept for compatibility, but YAML configuration is preferred
# See config/application.yml for main configuration

spring.application.name=dota-service
spring.profiles.active=dev
